#!/bin/sh
# A script to grab a screenshot of a selected section
# by Alex Chernika <me@chernika.dev>

set -e

AREA=$(slurp -d) \
    && grim -g "$AREA" - | wl-copy \
    && notify-send "Screenshot taken!" "Saved in clipboard."
