#!/bin/sh
# A dead-simple script to open the clipboard history generated with cliphist
# by Alex Chernika <me@chernika.dev>

cliphist list | sed 's|\t| |' | bemenu -p 'Clipboard:' | sed 's| |\t|' | cliphist decode | wl-copy
